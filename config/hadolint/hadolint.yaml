# Hadolint rule reference:
# https://github.com/hadolint/hadolint#rules
ignored:
  # Needs root during startup prep.
  - DL3002

  # Image refs come from build args.
  - DL3006

  # Python pins come from AWX requirements and our constraints file.
  - DL3013

  # We are not locking CentOS Stream RPM versions here.
  - DL3041

  # Separate RUN steps keep the Dockerfile easier to work on.
  - DL3059
