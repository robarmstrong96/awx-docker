# Unit Tests

This directory contains fast unit tests for the wrapper code.

These tests avoid full image builds. They cover small Python helpers, tracked
component defaults, and static runtime guardrails that should fail before an
image build starts.
