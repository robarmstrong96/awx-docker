# Unit Tests

This directory contains fast unit tests for the wrapper code.

These tests avoid full image builds. They cover small Python helpers and the
static runtime guardrails that should fail before an image build starts.
